[project]
name = "nuscenes-sandbox-backend"
version = "0.1.0"
description = "Serves nuScenes mini scenes to the viewer"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "numpy>=2.0",
    "pillow>=10.0",
    "torch>=2.14.0",
    "open3d>=0.19.0",
    "scipy>=1.18.1",
]

[tool.uv]
package = false
